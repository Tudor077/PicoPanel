<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- USB host is required: without it the phone cannot be the host for the
         panel. Marked not-required so the app still installs on a phone that
         lacks OTG, where the emulator remains fully usable. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- The alarm relay: one HTTP POST on the local network, to the desktop
         app, carrying the next alarm's time. The foreground service is not a
         choice - since Android 8 the "next alarm changed" broadcast only
         reaches a process that is already running, and an app with nothing
         running cannot be told anything.

         usesCleartextTraffic, on the application below, is not a choice
         either: from targetSdk 28 Android refuses plain HTTP outright, and
         the desktop app is a program on your own LAN with no certificate and
         no name to put one in. It threw IOException and said nothing else,
         which cost an evening. A network security config cannot help - it
         whitelists hostnames, and this is an address you type in. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Opens the app when the panel is plugged in. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <service
            android:name=".RelayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
